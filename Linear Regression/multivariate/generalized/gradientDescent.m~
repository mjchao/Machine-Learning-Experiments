function [ output_args ] = gradientDescent( thetas , x , y )
%GRADIENTDESCENT Performs the gradient descent algorithm
alpha = 0.01;
m = size( x , 1 );
n = size( x , 2 );

for i = 1:1000
    for j = 1:n
        
    end
end
end

